{% set classes = [
  'ex-button',
  style ? "ex-button--#{style}",
  size ? "ex-button--size-#{size}"
]  | join(' ') | trim %}

<button class="{{ classes }}" {{ disabled ? 'disabled' }}>{{ text }}</button>