<div class="flex items-center dark:bg-gray-900">
  <div class="flex-shrink-0">
    <a href="#">
      <span class="sr-only">Roel Aufderehar</span>
      {% if imgSrc %}<img class="h-10 w-10 rounded-full" src="{{ imgSrc }}" alt="" />{% endif %}
    </a>
  </div>
  <div class="ml-3">
    {% if children %}
    <p class="text-sm font-medium text-gray-900 dark:text-white">
      <a href="#" class="hover:underline">{{ children }}</a>
    </p>
    {% endif %}
    <div class="flex space-x-1 text-sm text-gray-500 dark:text-gray-400">
      {% if date %}<time datetime="2020-03-10">{{ date }}</time>{% endif %}
      {% if date and read %}<span aria-hidden="true">·</span>{% endif %}
      {% if read %}<span>{{ read }}</span>{% endif %}
    </div>
  </div>
</div>
