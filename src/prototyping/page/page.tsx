import * as React from 'react';
// import { CardProps } from '../../../dist/meta/react';


export const Page = ({children}) => {
  return (
    <>
      {children}
    </>
  );
};

export default Page;
